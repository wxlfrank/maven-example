image: docker:latest
services:
  - docker:dind

variables:
  SPRING_PROFILES_ACTIVE: gitlab-ci
#  MAVEN_CLI_OPTS: "-s .m2/settings.xml --batch-mode"
  MAVEN_OPTS: "-Dmaven.repo.local=.m2"

cache:
  paths:
    - .m2/repository/
    - target/

before_script:
    - apt-get update -y && apt-get upgrade -y

stages:
  - build
  - package
  - deploy

maven-build:
  image: maven:3-jdk-8
  stage: build
  script:
   - mvn $MAVEN_CLI_OPTS -U clean install
  artifacts:
    paths:
      - target/*.jar

docker-build:
  stage: package
  script:
   - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
   - docker build -t $CI_REGISTRY_IMAGE .
   - docker push $CI_REGISTRY_IMAGE

#k8s-deploy:
#  image: google/cloud-sdk
#  stage: deploy
#  script:
#   - echo "$GOOGLE_KEY" > key.json
#   - gcloud auth activate-service-account --key-file key.json
#   - gcloud config set compute/zone europe-west1-c
#   - gcloud config set project swagger-spring-webapi
#   - gcloud config set container/use_client_certificate True
#   - gcloud container clusters get-credentials swagger-spring-webapi
#   - kubectl delete secret registry.gitlab.com
#   - kubectl create secret docker-registry registry.gitlab.com --docker-server=https://registry.gitlab.com --docker-username=valerio --docker-password=$REGISTRY_PASSWD --docker-email=valerio.vinciarelli@ingv.it
#   - kubectl apply -f deployment.yml
